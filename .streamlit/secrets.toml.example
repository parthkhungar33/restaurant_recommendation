# Copy values into Streamlit Community Cloud → App settings → Secrets.
# Do not commit real keys.

GROQ_API_KEY = ""
# Optional: absolute or repo-relative path inside the deployment workspace.
# DB_PATH = "src/phase_1_ingestion/data/restaurants.db"
