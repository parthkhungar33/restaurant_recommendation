line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP"]

[lint.per-file-ignores]
# Streamlit secrets and DB_PATH must be applied before importing project settings.
"streamlit_app.py" = ["E402", "I001"]
